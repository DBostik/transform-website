node_modules/

.DS_Store
.env
.env.*
!.env.example

# iCloud conflict copies (files ending in " 2", " 3", etc.) - never commit these
* 2.*
* 3.*
* 4.*
*\ 2
*\ 3

# Scratch / quarantine folders - never commit these
_to_delete/
_preview/

# Files the Claude desktop app saves into the project folder - never commit
Claude outputs/
